import 'package:flutter/material.dart';

import '../web_view.dart';

class DesktopDevice extends StatelessWidget {
  const DesktopDevice({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Sections();
  }
}

class PhoneDevice extends StatelessWidget {
  const PhoneDevice({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Sections();
  }
}

class TabletDevice extends StatelessWidget {
  const TabletDevice({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Sections();
  }
}